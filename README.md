# GitDeploy

Auto git deploy for your projects.

## Install

Copy and rename config file:

    cp config.conf.sample config.conf
    
Edit:
    
    nano config.conf
    
Or
    
    vi config.conf
    
And run it!